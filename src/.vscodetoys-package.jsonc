{
  "contributes": {
    "commands": [
    ],
    "configuration": {
      "properties": {
        // Insert your configuration properties here
        "vstoys.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Global enable/disable for the VSCode Toys",
          "order": 100 // Always at the top
        }
      }
    },
    "keybindings": [
      // Insert your keybindings here
    ]
  }
  // Use this to enable proposed API features
  // "enabledApiProposals": [
  //   "quickPickSortByLabel"
  // ]
}
